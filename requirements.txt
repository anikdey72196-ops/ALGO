# ─────────────────────────────────────────────
# Automated Trading Bot — Dependencies
# ─────────────────────────────────────────────
# Install:  pip install -r requirements.txt
#
# For live MT5 trading on Windows:
#   pip install MetaTrader5
# MT5 library only works on Windows with the MT5 terminal installed.
# On Linux VPS, use Wine or run the bot on a Windows VPS.
# ─────────────────────────────────────────────

pydantic>=2.0
loguru>=0.7
apscheduler>=3.10
numpy>=1.24
pandas>=2.0

fastapi>=0.110.0
uvicorn>=0.28.0

# ML — Trap Detector (FVG / Sweep veto gate)
scikit-learn>=1.3
lightgbm>=4.0
joblib>=1.3

# MT5, AI & Config
python-dotenv>=1.0
MetaTrader5>=5.0.45
google-genai>=2.0

